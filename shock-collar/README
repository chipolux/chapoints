# ESP-01 Shock Collar Controller

## Requirements

* PlatformIO Core (should be able to run `pio run -t upload`)
* Create `include/secrets.h` with defines for `WIFI_SSID`, `WIFI_PASS`, etc.


## Build and Deploy

`pio run -t upload`
